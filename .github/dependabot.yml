version: 2

updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"

    groups:
      dependencies:
        dependency-type: "production"
      devDependencies:
        dependency-type: "development"
